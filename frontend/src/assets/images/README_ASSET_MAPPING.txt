This directory is expected to contain the marketing images used by home page sections.

Note: The mapping is done in:
- frontend/src/data/media.data.js

Keys like wallOne..wallNine are kept for backwards compatibility with existing home.data.js.

